# Helm Chart - RabbitMQ Integration

Helm chart for deploying onix-adapter with RabbitMQ. The same configuration works for both monolithic and microservice architectures.

*(Documentation coming soon)*


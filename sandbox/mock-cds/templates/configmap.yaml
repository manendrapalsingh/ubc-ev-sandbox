apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mock-cds.fullname" . }}-config
  labels:
    {{- include "mock-cds.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}


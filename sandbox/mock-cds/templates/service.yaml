apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-cds.fullname" . }}
  labels:
    {{- include "mock-cds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-cds.selectorLabels" . | nindent 4 }}


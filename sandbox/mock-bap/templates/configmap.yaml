apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mock-bap.fullname" . }}-config
  labels:
    {{- include "mock-bap.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}


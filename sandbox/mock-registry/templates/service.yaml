apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-registry.fullname" . }}
  labels:
    {{- include "mock-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-registry.selectorLabels" . | nindent 4 }}


apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-bpp.fullname" . }}
  labels:
    {{- include "mock-bpp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-bpp.selectorLabels" . | nindent 4 }}


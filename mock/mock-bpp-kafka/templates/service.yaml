apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-bpp-kafka.fullname" . }}
  labels:
    {{- include "mock-bpp-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-bpp-kafka.selectorLabels" . | nindent 4 }}

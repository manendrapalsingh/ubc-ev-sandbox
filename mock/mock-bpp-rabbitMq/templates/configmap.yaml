apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mock-bpp-rabbitmq.fullname" . }}-config
  labels:
    {{- include "mock-bpp-rabbitmq.labels" . | nindent 4 }}
data:
  config.yml: |
{{ toYaml .Values.config | indent 4 }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mock-bap-kafka.fullname" . }}-config
  labels:
    {{- include "mock-bap-kafka.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}

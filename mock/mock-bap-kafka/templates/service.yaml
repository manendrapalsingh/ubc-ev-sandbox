apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-bap-kafka.fullname" . }}
  labels:
    {{- include "mock-bap-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-bap-kafka.selectorLabels" . | nindent 4 }}
